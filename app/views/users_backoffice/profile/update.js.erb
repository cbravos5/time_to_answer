$('#ImgModal').modal('hide');

document.getElementById('infoAvatar').src = "<%= url_for(@user.user_profile.avatar) %>"
document.getElementById('topAvatar').src = "<%= url_for(@user.user_profile.avatar) %>"
document.getElementById('modalAvatar').src = "<%= url_for(@user.user_profile.avatar) %>"



$.bootstrapGrowl('Profile Picture Changed', {
    ele: 'body', // which element to append to
    type: 'success', // (null, 'info', 'danger', 'success')
    offset: {from: 'top', amount: 20}, // 'top', or 'bottom'
    align: 'center', // ('left', 'right', or 'center')
    delay: 4000, // Time while the message will be displayed. It's not equivalent to the *demo* timeOut!
    allow_dismiss: true, // If true then will display a cross to close the popup.
    stackup_spacing: 10 // spacing between consecutively stacked growls.
  });